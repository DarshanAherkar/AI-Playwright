name: Smoke Test Runner with Smart Selection

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number
        required: true
        type: string
      pr_title:
        description: Pull request title
        required: true
        type: string
      pr_url:
        description: Pull request URL
        required: true
        type: string
      source_repo:
        description: Source repository
        required: true
        type: string
      head_sha:
        description: Source commit SHA
        required: true
        type: string
      regression_mode:
        description: Run as full regression (updates cache baseline)
        required: false
        type: boolean
        default: false
  schedule:
    # Run full regression nightly at 2 AM UTC
    - cron: '0 2 * * *'

jobs:
  smart-test-run:
    if: github.event_name == 'workflow_dispatch'
    runs-on: self-hosted

    permissions:
      contents: read
      actions: write
      pull-requests: read

    steps:
      - name: Checkout tester repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Verify Ollama is running
        shell: powershell
        continue-on-error: true
        run: |
          Write-Host "[ACTION] Checking Ollama service..."
          try {
            Invoke-RestMethod -Uri "http://localhost:11434/api/tags" -TimeoutSec 5 | Out-Null
            Write-Host "[OK] Ollama is running"
          } catch {
            Write-Host "[WARN] Ollama not responding. Attempting to start ollama serve..."
            try {
              Start-Process -FilePath "ollama" -ArgumentList "serve" -WindowStyle Hidden -PassThru | Out-Null
              Start-Sleep -Seconds 5
              Invoke-RestMethod -Uri "http://localhost:11434/api/tags" -TimeoutSec 5 | Out-Null
              Write-Host "[OK] Ollama started successfully"
            } catch {
              Write-Host "[WARN] Unable to auto-start Ollama. Ensure ollama is installed and run: ollama serve"
            }
          }

      - name: Start Smart Test Selector
        shell: powershell
        run: |
          $pythonExe = (Get-Command python).Source
          $serverScript = "${{ github.workspace }}\ollama-test-selector.py"

          Write-Host "[ACTION] Clearing any stale selector on port 8000..."
          $listeners = Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue
          if ($listeners) {
            $pids = $listeners | Select-Object -ExpandProperty OwningProcess -Unique
            foreach ($procId in $pids) {
              try {
                Stop-Process -Id $procId -Force -ErrorAction Stop
                Write-Host "[OK] Stopped stale process PID: $procId"
              } catch {
                Write-Host "[WARN] Failed to stop PID ${procId}: $($_.Exception.Message)"
              }
            }
          } else {
            Write-Host "[OK] No stale process found on port 8000"
          }
          
          Write-Host "[ACTION] Starting Ollama Smart Test Selector..."
          
          # Start as a real detached Windows process (survives across steps)
          Start-Process `
            -FilePath $pythonExe `
            -ArgumentList $serverScript, "8000" `
            -WindowStyle Hidden `
            -PassThru | Out-Null
          
          Write-Host "[OK] Server process launched"
          Start-Sleep -Seconds 3
        continue-on-error: true

      - name: Verify smart-test-engine availability
        shell: python
        env:
          PYTHONIOENCODING: utf-8
        run: |
          import urllib.request
          import urllib.error
          import time
          
          print("[ACTION] Verifying Smart Test Engine availability...")
          max_retries = 10
          retry_delay = 3
          
          for attempt in range(1, max_retries + 1):
              try:
                  request = urllib.request.Request(
                      "http://localhost:8000/health",
                      method="GET",
                      headers={"User-Agent": "smart-test-selection-runner"}
                  )
                  with urllib.request.urlopen(request, timeout=5) as response:
                      result = response.read().decode("utf-8")
                      print(f"[OK] Smart Test Engine is running and healthy")
                      break
              except Exception as e:
                  if attempt < max_retries:
                      print(f"   Attempt {attempt}/{max_retries}: Waiting for engine... ({type(e).__name__})")
                      time.sleep(retry_delay)
                  else:
                      print(f"[WARN] Smart Test Engine not responding after {max_retries} attempts")
                      print(f"   Error: {type(e).__name__}: {e}")
                      print(f"   Will use fallback file-based test selection")
        continue-on-error: true

      - name: Fetch changed files from source PR
        id: changed-files
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          SOURCE_REPO: ${{ github.event.inputs.source_repo }}
          PR_NUMBER: ${{ github.event.inputs.pr_number }}
          SOURCE_REPO_TOKEN: ${{ secrets.SOURCE_REPO_TOKEN }}
        run: |
          import json
          import os
          import urllib.error
          import urllib.request

          repo = os.environ["SOURCE_REPO"]
          token = os.environ.get("SOURCE_REPO_TOKEN", "")
          pr_number = os.environ["PR_NUMBER"]
          url = f"https://api.github.com/repos/{repo}/pulls/{pr_number}/files?per_page=100"

          headers = {
            "Accept": "application/vnd.github+json",
            "User-Agent": "smart-test-selection-runner"
          }
          if token:
            headers["Authorization"] = f"Bearer {token}"

          changed = []
          page = 1
          try:
            while True:
              request = urllib.request.Request(f"{url}&page={page}", headers=headers)
              with urllib.request.urlopen(request) as response:
                payload = json.loads(response.read().decode("utf-8"))
              if not payload:
                break
              changed.extend(item["filename"] for item in payload if item.get("filename"))
              if len(payload) < 100:
                break
              page += 1
          except urllib.error.HTTPError as exc:
            print(f"Warning: PR file fetch failed with HTTP {exc.code}; continuing with empty change list")
          except Exception as exc:
            print(f"Warning: PR file fetch failed ({exc}); continuing with empty change list")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as stream:
            stream.write(f"changed_files={','.join(changed)}\n")

      - name: Run smart selection engine
        id: select-tests
        continue-on-error: true
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          CHANGED_FILES: ${{ steps.changed-files.outputs.changed_files }}
          PR_NUMBER: ${{ github.event.inputs.pr_number }}
          PR_TITLE: ${{ github.event.inputs.pr_title }}
          PR_URL: ${{ github.event.inputs.pr_url }}
        run: |
          import json
          import os
          import re
          import urllib.error
          import urllib.request

          print("=" * 80)
          print("SMART TEST SELECTION ENGINE - scikit-learn + Historical RAG")
          print("=" * 80)

          changed_files_str = os.environ.get("CHANGED_FILES", "")
          changed_files = [f.strip() for f in changed_files_str.split(",") if f.strip()]

          workspace = os.environ.get("GITHUB_WORKSPACE", os.getcwd())
          tests_dir = os.path.join(workspace, "tests")
          if os.path.isdir(tests_dir):
            available_tests = sorted(
              f"tests/{name}" for name in os.listdir(tests_dir)
              if name.endswith(".spec.js")
            )
          else:
            available_tests = ["tests/pom-smoke.spec.js"]

          print(f"\n[INFO] Changed files detected: {len(changed_files)}")
          for changed_file in changed_files[:5]:
            print(f"   - {changed_file}")
          if len(changed_files) > 5:
            print(f"   ... and {len(changed_files) - 5} more")

          payload = {
            "pr_number": os.environ.get("PR_NUMBER", "0"),
            "pr_title": os.environ.get("PR_TITLE", ""),
            "pr_description": os.environ.get("PR_URL", ""),
            "changed_files": changed_files,
            "workspace": workspace,
            "request_type": "smart_selection"
          }

          api_url = "http://localhost:8000/api/v1/select-tests"
          fallback_used = False
          selected_tests = []
          explanations = []

          try:
            print(f"\n[ACTION] Calling Smart Test Engine API: {api_url}")
            request_data = json.dumps(payload).encode("utf-8")
            request = urllib.request.Request(
              api_url,
              data=request_data,
              headers={
                "Content-Type": "application/json",
                "User-Agent": "smart-test-selection-runner"
              },
              method="POST"
            )

            with urllib.request.urlopen(request, timeout=60) as response:
              result = json.loads(response.read().decode("utf-8"))
              print("\n[OK] Smart Test Engine Response:")
              print(f"   Status: {result.get('status', 'unknown')}")

              selected_tests = result.get("selected_tests", [])
              explanations = result.get("explanations", [])

              print(f"\n[RESULT] ML-Ranked Test Selection ({len(selected_tests)} tests):")
              for i, test in enumerate(selected_tests, 1):
                print(f"\n   {i}. {test}")
                if i <= len(explanations):
                  exp = explanations[i - 1]
                  priority = exp.get("priority_score", 0)
                  print(f"      Priority: {priority:.2f}/1.0")
                  print(f"      Evidence: {exp.get('evidence', 'No evidence')}")
                  print(f"      Reasoning: {exp.get('reasoning', 'No reasoning')}")

          except urllib.error.URLError as error:
            print(f"\n[WARN] Smart Engine API unavailable ({error.reason})")
            fallback_used = True
          except urllib.error.HTTPError as error:
            print(f"\n[WARN] Smart Engine API error: HTTP {error.code}")
            try:
              print(f"   Response: {error.read().decode('utf-8')}")
            except Exception:
              pass
            fallback_used = True
          except Exception as error:
            print(f"\n[WARN] Error calling Smart Engine: {type(error).__name__}: {error}")
            fallback_used = True

          if selected_tests:
            selected_tests = [test for test in selected_tests if test in available_tests]

          if selected_tests and available_tests:
            ratio = len(selected_tests) / len(available_tests)
            if ratio >= 0.8 and len(changed_files) <= 20:
              print("\n[GUARDRAIL] Smart Engine selected almost full suite for limited changes.")
              print(f"   selected={len(selected_tests)} of available={len(available_tests)}")
              fallback_used = True
              selected_tests = []

          if fallback_used or not selected_tests:
            print("\n[FALLBACK] Using dynamic keyword-based test selection:")
            selected_tests = set()
            smoke_test = "tests/pom-smoke.spec.js"

            for changed_file in changed_files:
              if changed_file.startswith("tests/") and changed_file.endswith(".spec.js"):
                selected_tests.add(changed_file)
                print(f"   - {changed_file} (test file)")
                continue

              if changed_file.startswith("mock-app/") and (
                changed_file.endswith(".html") or changed_file.endswith(".css")
              ):
                selected_tests.add(smoke_test)
                print(f"   - {changed_file} -> smoke test (UI changed)")
                continue

              if changed_file.startswith("src/"):
                file_tokens = set(re.findall(r"[a-z0-9]+", changed_file.lower()))
                matched = False
                for test in available_tests:
                  test_tokens = set(re.findall(r"[a-z0-9]+", test.lower()))
                  stem_tokens = test_tokens - {"tests", "spec", "js", "pom", "smoke"}
                  if stem_tokens and file_tokens.intersection(stem_tokens):
                    selected_tests.add(test)
                    matched = True
                    print(f"   - {changed_file} -> {test} (token match)")
                if not matched:
                  selected_tests.add(smoke_test)
                  print(
                    f"   - {changed_file} -> smoke test "
                    "(src changed, no direct token match)"
                  )

            if not selected_tests:
              selected_tests = {smoke_test}
              print("   - Default: pom-smoke.spec.js")

            selected_tests = sorted(list(selected_tests))

          # Absolute safety: never allow duplicates in execution list.
          selected_tests = list(dict.fromkeys(selected_tests))

          print(f"\n{'=' * 80}")
          print(f"[SUMMARY] FINAL SELECTION: {len(selected_tests)} test(s)")
          for test in selected_tests:
            print(f"   [+] {test}")
          print(f"{'=' * 80}\n")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as stream:
            stream.write(f"selected_tests={' '.join(selected_tests)}\n")
            stream.write(f"fallback_mode={'true' if fallback_used else 'false'}\n")

      - name: Build executable test list
        id: plan
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          SELECTED_TESTS: ${{ steps.select-tests.outputs.selected_tests }}
        run: |
          import os
          
          # Get tests from smart selection engine
          selected_tests_str = os.environ.get("SELECTED_TESTS", "")
          selected_tests = selected_tests_str.split() if selected_tests_str else ["tests/pom-smoke.spec.js"]
          selected_tests = list(dict.fromkeys(selected_tests))
          
          print(f"Tests to execute: {selected_tests}")
          
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as stream:
              stream.write("selected_tests=" + " ".join(selected_tests) + "\n")

      - name: Install Playwright dependencies
        run: |
          npm install
          npx playwright install

      - name: Display test selection summary
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          FALLBACK_MODE: ${{ steps.select-tests.outputs.fallback_mode }}
          SELECTED_TESTS: ${{ steps.plan.outputs.selected_tests }}
        run: |
          import os
          
          fallback = os.environ.get("FALLBACK_MODE") == "true"
          selected = os.environ.get("SELECTED_TESTS", "").split()
          
          print("\n" + "="*80)
          print("TEST EXECUTION SUMMARY")
          print("="*80)
          
          if fallback:
              print("\n[MODE] File-Based Fallback (Smart Engine unavailable)")
          else:
              print("\n[MODE] ML-Ranked Smart Selection (scikit-learn + RAG + TinyLlama)")
          
          print(f"\n[TESTS] Tests to Execute ({len(selected)}):")
          for i, test in enumerate(selected, 1):
              print(f"   {i}. {test}")
          
          print("\n" + "="*80 + "\n")

      - name: Execute selected tests
        run: |
          npx playwright test ${{ steps.plan.outputs.selected_tests }}

      - name: Generate comparison report
        if: always()
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          SELECTED_TESTS: ${{ steps.plan.outputs.selected_tests }}
          IS_REGRESSION_INPUT: ${{ github.event.inputs.regression_mode == 'true' }}
          IS_SCHEDULE: ${{ github.event_name == 'schedule' }}
          CACHE_HIT: 'false'
          FULL_SUITE_CACHE_DIR: C:\ai-playwright-cache\full-suite-regression
        run: |
          import subprocess
          import sys
          import os

          workspace = os.environ.get("GITHUB_WORKSPACE", os.getcwd())
          os.chdir(workspace)

          is_regression = (os.environ.get("IS_REGRESSION_INPUT", "false").lower() == "true" or
                          os.environ.get("IS_SCHEDULE", "false").lower() == "true")
          cache_hit = os.environ.get("CACHE_HIT", "false").lower() == "true"
          cache_dir = os.environ.get("FULL_SUITE_CACHE_DIR", os.path.join(workspace, ".ci-cache", "full-suite-regression"))
          cache_file = os.path.join(cache_dir, "latest-full-suite.json")
          cache_file_exists = os.path.exists(cache_file)
          
          print("\n" + "="*80)
          print(f"[INFO] Workspace: {workspace}")
          print(f"[INFO] Regression: {is_regression}, Cache Hit: {cache_hit}, Baseline File: {cache_file_exists}")
          print("="*80 + "\n")
          
          if is_regression:
            # REGRESSION RUN: Always run full suite
            command = [sys.executable, "scripts/generate_comparison_report.py", "--regression"]
            print("[MODE] REGRESSION: Full suite + cache update")
          elif cache_hit or cache_file_exists:
            # NORMAL PR WITH BASELINE: Use cached baseline
            command = [sys.executable, "scripts/generate_comparison_report.py"]
            print("[MODE] PR with BASELINE: Smart tests vs cached baseline")
          else:
            # NORMAL PR WITHOUT CACHE: Initialize cache first
            print("[MODE] FIRST RUN: Initializing cache...")
            init_cmd = [sys.executable, "scripts/generate_comparison_report.py", "--regression"]
            init_result = subprocess.run(init_cmd, check=False, cwd=workspace)
            
            if init_result.returncode == 0:
              if os.path.exists(cache_file):
                print("[SUCCESS] Cache initialized")
              else:
                print(f"[ERROR] Regression finished but baseline file missing: {cache_file}")
                sys.exit(1)
              # Now run comparison
              command = [sys.executable, "scripts/generate_comparison_report.py"]
            else:
              print("[ERROR] Cache init failed")
              sys.exit(1)
          
          subprocess.run(command, check=False, cwd=workspace)

      - name: Debug cache files
        if: always()
        shell: python
        env:
          FULL_SUITE_CACHE_DIR: C:\ai-playwright-cache\full-suite-regression
        run: |
          from pathlib import Path
          import os

          cache_dir = Path(os.environ.get("FULL_SUITE_CACHE_DIR", ".ci-cache/full-suite-regression"))
          cache_file = cache_dir / "latest-full-suite.json"

          print(f"[INFO] cache_dir exists: {cache_dir.exists()} -> {cache_dir}")
          print(f"[INFO] cache_file exists: {cache_file.exists()} -> {cache_file}")
          if cache_dir.exists():
            print("[INFO] cache_dir contents:")
            for item in sorted(cache_dir.glob("*")):
              print(f"  - {item.name}")

      - name: Upload comparison report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smart-selection-comparison-report
          path: comparison-report/
          retention-days: 30

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  # Scheduled nightly regression: Full suite + cache update (no PR context needed)
  nightly-regression:
    if: github.event_name == 'schedule'
    runs-on: self-hosted

    permissions:
      contents: read
      actions: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Install Playwright dependencies
        run: |
          npm install
          npx playwright install

      - name: Run full regression with cache update
        shell: python
        env:
          PYTHONIOENCODING: utf-8
          FULL_SUITE_CACHE_DIR: C:\ai-playwright-cache\full-suite-regression
        run: |
          import subprocess
          import sys

          print("=" * 80)
          print("NIGHTLY FULL REGRESSION RUN")
          print("=" * 80)
          print("")
          print("[INFO] This run:")
          print("  1. Executes the full Playwright test suite")
          print("  2. Caches results for all future PR comparisons")
          print("  3. Establishes baseline for the next 24 hours")
          print("")

          # Run in regression mode: full suite + cache
          command = [sys.executable, "scripts/generate_comparison_report.py", "--regression"]
          result = subprocess.run(command, check=False)
          
          sys.exit(result.returncode)

      - name: Upload regression report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-regression-report
          path: comparison-report/
          retention-days: 90

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-regression-playwright-report
          path: playwright-report/
          retention-days: 90
